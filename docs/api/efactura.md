# e-Factura API

The client and its value types. The generated UBL 2.1 / CIUS-RO models
(`anafpy.efactura.Invoice`, `CreditNote`, and the `anafpy.efactura.ubl` package)
are intentionally not documented here — they mirror the UBL schema one-to-one.

::: anafpy.efactura.client

::: anafpy.efactura.models
